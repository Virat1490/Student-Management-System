<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.virat1490.majorproject">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/student_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/student_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.MajorProject">
        <activity android:name=".Register">

        </activity>
        <activity android:name=".fetch" />
        <activity android:name=".Select_student" />
        <activity android:name=".Home" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>